#include<stdio.h>

int main()
{
	/*
	int arr[5] = { 2,4,6,8,10 };
	int(*p)[5];
	p = &arr;

	for (int i = 0; i < 5; i++) {
		printf("arr[%d] = %d\n", i, (*p)[i]);
	}

	char text[100] = "abcdefghijk";
	char(*pt)[100];
	pt = &text;
	for (int i = 0; i < 100; i++) {
		printf("%c", (*pt)[i]);
	}
	int arr[2][3] = { {1,2,3}, {9,10,11} };
	// 문자열에 대한 2차원배열
	char zoo[4][20] = { "dog","cat","lion","tiger" };
	char(*pz)[20] = zoo;

	for(int i=0;i<4;i++){
	}
	
}
*/
	/*
	char text[10] = "abcdefg";
	char(*p)[10];
	//C언어에서 text는 주솟값
	//C++ 명시적으로 쓰세요. 뭐를? &를. 그리고 배열의 행을.
	p = &text;
	*/
	/*
	char zoo[4][20] = { "dog","cat","tiger","lion" };
	char(*p)[20];
	p = &zoo[0];
	*/
	




	


	

	
	
	return 0;
}